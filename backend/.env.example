# Soil Health Backend - Environment Configuration

# Flask Configuration
FLASK_ENV=development
FLASK_DEBUG=True

# Server Configuration
SERVER_HOST=0.0.0.0
SERVER_PORT=5000

# Model Configuration
MODEL_DIR=models
DATA_DIR=data
CSV_PATH=data/data.csv

# ML Parameters
ANOMALY_CONTAMINATION=0.05
RF_ESTIMATORS=100
RF_MAX_DEPTH=10
IF_ESTIMATORS=100

# CORS Configuration (for production, restrict to frontend domain)
CORS_ORIGINS=http://localhost:3000,http://localhost:5173,http://localhost

# Logging
LOG_LEVEL=INFO
